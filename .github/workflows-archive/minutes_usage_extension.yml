name: WNBA Minutes Usage Extension

on:
  workflow_dispatch:
    inputs:
      date:
        description: 'Target date (YYYY-MM-DD), leave blank for latest predictions file'
        required: false
        default: ''
  workflow_run:
    workflows: ["WNBA Daily Pipeline"]
    types: [completed]

concurrency:
  group: wnba-extension-writes
  cancel-in-progress: false

jobs:
  run-minutes-usage:
    if: ${{ github.event_name == 'workflow_dispatch' || github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Sync latest repo
        run: git pull --rebase origin main

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Resolve target date
        run: |
          if [ -n "${{ github.event.inputs.date }}" ]; then
            echo "TARGET=${{ github.event.inputs.date }}" >> $GITHUB_ENV
          else
            latest=$(ls predictions/predictions_*.json 2>/dev/null | sed -E 's/.*predictions_([0-9-]+).json/\1/' | sort | tail -1)
            echo "TARGET=${latest}" >> $GITHUB_ENV
          fi

      - name: Validate syntax
        run: python -m py_compile minutes_usage_intelligence.py patch_minutes_usage_ui.py build_dashboard.py validate_dashboard.py

      - name: Build Minutes Usage Intelligence
        run: python minutes_usage_intelligence.py --date ${{ env.TARGET }}

      - name: Rebuild dashboard
        run: python build_dashboard.py

      - name: Patch Minutes Usage UI
        run: python patch_minutes_usage_ui.py

      - name: Validate dashboard
        run: python validate_dashboard.py

      - name: Commit and push
        run: |
          git config user.name "WNBA Model Bot"
          git config user.email "actions@github.com"
          find . -type d -name __pycache__ -prune -exec rm -rf {} +
          git status --short
          git add -A
          git diff --cached --quiet && echo "No changes to commit" && exit 0
          git commit -m "Minutes usage intelligence update - ${{ env.TARGET }}"
          git push
